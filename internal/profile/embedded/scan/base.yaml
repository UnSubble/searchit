schema: 1

name: scan/base
tool: scan

description: Deterministic baseline profile that restores Searchit's built-in scan defaults

author: searchit
license: MIT
homepage: https://github.com/unsubble/searchit

tags:
  - base
  - baseline
  - default

depends: []

experimental: false

config:
  threads: 32
  timeout: 10
  connect-timeout: 3
  exclude-status: "404"
  recurse-on: "200,301,302,403"
  random-agent: false
