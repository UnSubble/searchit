package fuzz

import (
	"github.com/unsubble/searchit/internal/config"
)

// Overlay represents a partial fuzz configuration overlay.
// It is aliased to config.FuzzOverlay to maintain single-source definition.
type Overlay = config.FuzzOverlay
